# CS-FIVE Top 10 — Plano de implementação

> Deriva de `2026-06-10-top10-design.md`. Ordem por risco: valida a fonte de dados antes de codar o jogo.

## Fase 0 — Sondar a Liquipedia (de-risk) ✅
- [x] Achado: dados ranqueados NÃO estão no wikitext (LPDB renderiza server-side). Caminho: `action=parse`
      (HTML renderizado, grátis, 1 req/30s) das páginas agregadas **Majors/Team_Statistics** e
      **Majors/Player_Statistics**.
- [x] Prize money **adiado**: `Statistics/Prize_money` não existe sob esse título.
- [x] Mapeamento entidade→id validado (teams.json + aliases; players.json normalizado+leetspeak).

## Fase 1 — Harvest de stats ✅
- [x] `scripts/harvest-top10-stats.mjs` (compliant: UA, parse 30s, gzip, cache). Modo `--test`.
- [x] `public/data/top10-stats.json` — 7 tabelas (team titles/finals/podiums/appearances; player
      titles/appearances/mvps), entidades resolvidas a id conhecido.

## Fase 2 — Build ✅
- [x] `scripts/build-top10.mjs` → `public/data/top10-schedule.json` (top 10 + aliases, ordem
      determinística por hash). Validação: ≥10 entradas (desvio: empate no 10º é aceito com desempate
      estável por id, não dropa). Geração ficou no build (sem `lib/top10/themes.ts`), como o Grid.

## Fase 3 — Motor + matching ✅
- [x] `lib/top10/match.ts` (nome/abbr + leetspeak), `lib/top10/schedule.ts` (`getTop10ForPuzzle`),
      `lib/stats/top10.ts` (espelha grid). Testes em `tests/top10.test.ts` (5).

## Fase 4 — UI ✅
- [x] `app/top10/page.tsx` — tela CS-FIVE: tema, 10 linhas (logo/foto+bandeira reais), barra com
      flash verde/vermelho/azul, estado final + compartilhar.
- [x] `harvest:top10` e `build:top10` no package.json. Validado: tsc, 15 testes, next build.

## Pendências futuras (não-MVP)
- `majorStandings` por evento (colocação top 10 de cada Major) — principal alavanca de variedade.
- Prize money (achar a página correta na Liquipedia).

## Fora de escopo (YAGNI): contas, placar online, temas subjetivos (HLTV), ordenar manualmente.

## Atribuição: dados factuais de Major da Liquipedia (CC-BY-SA).
